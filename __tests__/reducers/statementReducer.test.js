describe('Statement Reducer', () => {
  test('should return state if no action is recognized', () => {
    expect(initialState.statement).toEqual("I'm alive!");
  });

  // test('is not sour', () => {
  //   expect(myBeverage.sour).toBeFalsy();
  // });
});